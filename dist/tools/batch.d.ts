/**
 * knowledge_batch - 批量操作
 *
 * 支持批量：
 * - 删除
 * - 更新标签
 * - 添加标签
 */
import type { McpServer } from "@modelcontextprotocol/server";
export declare function registerBatchTool(server: McpServer): void;
